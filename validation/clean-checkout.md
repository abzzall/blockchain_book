# Clean-checkout validation record

Generated by copying only the files tracked by git into an empty directory,
then running the two commands the README quick start documents. No build
output, dependency tree, or cache from a working tree was present.

| Field | Value |
| --- | --- |
| Date | 2026-09-12 |
| Validated commit | `b51b4ad63a888c76bbecb494b9e4360117f6fcd7` |
| Tracked files | 373 |
| Operating system | Ubuntu 24.04.4 LTS x86_64 |
| Python | 3.12.3 |
| Node.js | v24.15.0 |
| npm | 11.12.1 |
| Foundry | 1.8.1 |
| Solidity (pinned) | 0.8.37 |
| `npm ci` | exit 0 |
| `./scripts/check-all.sh` | exit 0 |

## Result

```
All companion-code checks passed.
```

| Suite | Tests |
| --- | --- |
| Python (9 sample directories) | 361 |
| Chapter 21 JavaScript samples | 11 |
| Hardhat chapters 23, 25, 27 | 24 |
| Lab workspaces | 75 |
| Foundry (7 projects) | 112 |
| **Total** | **583 passing, 0 failing** |

`lab-12` additionally ships 18 acceptance tests that fail by design under
`npm run accept`; the student writes the implementation that makes them pass.
They are not part of `check-all.sh` and are not counted above.

## Status of this record

This run validates commit `b51b4ad63a888c76bbecb494b9e4360117f6fcd7`
using only files tracked by git. The follow-up commit that records this hash
changes this evidence file only, not the executable tree that was tested.

The CI workflow in `.github/workflows/check.yml` repeats the same commands for
every pushed commit and retains the output as a build artefact for 90 days. Its
run at the eventual release commit is the final release record.

## Reproducing this record

```bash
dest=$(mktemp -d)
git ls-files -z | xargs -0 -I{} cp --parents {} "$dest"/
cd "$dest" && npm ci && ./scripts/check-all.sh
```
