# Printed by `npm run deploy:local` in ../contract
VITE_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
# The block the contract was deployed in. The interface scans for logs from
# here, not from zero: on a public chain, scanning from zero is not an option.
VITE_DEPLOY_BLOCK=0
